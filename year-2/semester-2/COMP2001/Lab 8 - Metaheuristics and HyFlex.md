

Roulette Wheel Selection (fitness-proportionate selection) is a reinforcement learning-based selection method. 

When a heuristic is applied, if its result improves, then the its "score" is incremented, increasing its probability to be selected. 